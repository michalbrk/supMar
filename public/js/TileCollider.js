class TileResolver {
    constructor(matrix, tileSize = 16) {
        this.matrix = matrix;
    }
}

export default class tileCollider {
    constructor(tiles) {
        this.tiles = tiles;
    }
    
    test(entity) {
        
    }
}